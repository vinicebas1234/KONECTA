"""Experimentos de diagnóstico do Libras Learning Agent — não é código de produção.

Ciclo 9: calibração cross-signer (`calibration_cycle9.py`). Nada aqui é
importado por `ml/features.py`, `ml/dtw.py` ou `ml/training.py` — a relação é
inversa (o experimento importa e reaproveita a produção), então adicionar
código aqui nunca muda o comportamento do resto do LLA.
"""
