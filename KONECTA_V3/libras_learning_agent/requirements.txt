# Libras Learning Agent — dependências do Ciclo 0 (fundação)
# Instalar: pip install -r libras_learning_agent/requirements.txt
# Mesmas versões mínimas de app_backend/requirements.txt, para não introduzir
# divergência no ecossistema mesmo rodando em venv separada.

sqlalchemy>=2.0.0
alembic>=1.12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Ciclo 1 (visão computacional) — mesmas faixas mínimas do requirements.txt
# da raiz do KONECTA_V3, para não introduzir divergência de versão.
numpy>=1.24.0,<2.0
opencv-python>=4.7.0
mediapipe>=0.8.11

# Ciclo 3 (pesquisa web) — busca no DuckDuckGo, gratuita e sem API key/cadastro.
ddgs>=9.0.0
